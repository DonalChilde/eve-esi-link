"""Helper modules for schema fetch, shape checks, and file load conventions.

These helpers intentionally keep transport, file-shape detection, and loader logic
separate so CLI and library code can reuse the same schema contracts.
"""
